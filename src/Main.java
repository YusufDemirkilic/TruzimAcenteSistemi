import Core.Db;
import Core.Helper;
import View.LoginView;

public class Main {
    public static void main(String[] args) {
      // LoginView loginView=new LoginView();
      //  Db db=new Db();
        Helper.setTheme();
        LoginView loginView=new LoginView();
    }
}
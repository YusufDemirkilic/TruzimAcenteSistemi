import Core.Db;
import View.LoginView;

public class Main {
    public static void main(String[] args) {
      // LoginView loginView=new LoginView();
      //  Db db=new Db();
        LoginView loginView=new LoginView();
    }
}